# Gray Boxing
A boxing game where your character mimics your own motions.

## Development Environment Setup

### Prerequisites

Before setting up the development environment, ensure you have the following installed:

- **Godot Engine** (latest)
- **UV (Python project manager)** (latest)

### Setup

1. Clone the repository locally:
    ```bash
    git clone git@github.com:FishyBusinessYT/gray-boxing.git
    ```
2. Navigate to the `py/` directory:
    ```bash
    cd ./gray-boxing/py
    ```
3. Setup python development environment using UV:
    ```bash
    uv sync
    ```

## Running the Game
### [[Complete this section once we can export builds]]
